# Railway / Nixpacks — laisser le provider PHP installer Composer.
# Ne PAS lister "composer" ni "mysql_client" (variables Nix inexistantes).

[variables]
NIXPACKS_PHP_VERSION = "8.2"

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
